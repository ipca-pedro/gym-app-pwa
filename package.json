{
  "name": "gym-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "python -m http.server 8080",
    "worker:dev": "wrangler dev api/worker.js --port 8787",
    "worker:deploy": "wrangler deploy",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}